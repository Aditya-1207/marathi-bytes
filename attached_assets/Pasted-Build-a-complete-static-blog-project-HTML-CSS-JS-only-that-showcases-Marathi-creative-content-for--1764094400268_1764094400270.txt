Build a complete static blog project (HTML/CSS/JS only) that showcases Marathi creative content for "प्राजक्तप्रभा". Output must be a GitHub-ready repository that can be published via GitHub Actions to GitHub Pages. Deliverables: full file tree, all HTML/CSS/JS files, a small Node.js helper script to auto-generate content index, and a GitHub Actions workflow for publishing.

Requirements & UX:
- Title/Header: Big bold agency name "प्राजक्तप्रभा". Sub-heading line (English/Marathi):
  "Welcome to my blog, where I share my poetry, passion for dance, and more. Writing, dancing, and humming are my joys. Follow me on Instagram and YouTube for a glimpse into my world. If you love self-expression and positivity, you'll enjoy my content. Subscribe for more of what makes you happy!"
- Categories: must support any number of categories automatically. Start with:
  - Poetry (काव्य-संग्रह)
  - Articles (आठवणींचा ठेवा)
  - Ukhane (उखाणे)
  Each category appears as a top navigation header.
- Home page: grid of latest content cards — 2 cards per row on desktop, responsive to 1 per row on mobile. Show first 10 items, then a "View more..." button to load next 10 (pagination or incremental loading). Cards show thumbnail, title (Marathi where provided), short excerpt, tags, category label, publish date.
- Category pages: clicking a category shows items for that category (multi-page same pagination style).
- Single content page: show full content, metadata (title, date, tags, category), social share icons (linking to Facebook/Twitter/WhatsApp share), and navigation back to category/home.
- Tag filtering: allow clicking a tag on any page to show filtered list (with same card layout & pagination).
- Search box: client-side search across titles and excerpts.
- Social links area (with images/icons) that redirect to Instagram, YouTube, and Facebook. Show active thumbnails for each (use included placeholder images).
- About section: short biography paragraph in Marathi + English. Button to contact.
- Photo Slideshow/Carousel: an autoplaying slideshow on the home page (or in About) that links each photo to the related Instagram post (the blog should use the URLs supplied in the content metadata).
- Auto-discovery of categories: the site must automatically create navigation/headers when a new folder is added under /src (e.g. adding `/src/newcategory` with files should create header and pages). Implement this by providing a small Node script that runs at build time to generate a `content.json` manifest from the `src/` folder structure. The site reads `content.json` at runtime.
- File format: each content file (HTML or Markdown) must include a small JSON metadata block at top using this marker format:
  <!--METADATA
  {
    "title": "कविता शीर्षक",
    "date": "2025-11-22",
    "excerpt": "लघु परिचय...",
    "category": "Poetry",
    "categoryLabel": "काव्य-संग्रह",
    "tags": ["प्रेम", "निसर्ग"],
    "thumbnail": "assets/thumbs/poem1.jpg",
    "instagram": "https://instagram.com/p/...",
    "youtube": "",
    "facebook": ""
  }
  -->
  Then the body (HTML/Markdown).
- Internationalization: UI labels for nav and UI elements should be Marathi-first with English fallback (example: Poetry (काव्य-संग्रह)).
- Accessibility & performance: images lazy-load, ARIA attributes for nav and slideshow, semantic HTML. Keep CSS minimal and mobile-first. No external JS frameworks. If any icons are needed use inline SVG.
- Styling: clean, modern, airy typography; 2-column card grid on wide screens; cards with subtle shadow and rounded corners. Make header sticky on desktop.
- Static only: must not require server runtime. All dynamic behavior should be client-side using `content.json`. The Node helper is only for the build step (local or GitHub Actions) to produce the `content.json`.

Developer/Repo structure to generate:
- `/`  (root)
  - README.md (instructions how to add content, how to run the Node helper, how to deploy)
  - index.html
  - category.html (template)
  - post.html (template)
  - assets/
    - css/
      - styles.css
    - js/
      - main.js
      - loader.js (responsible for reading content.json and rendering)
    - images/ (placeholders & social icons)
  - src/
    - poetry/  (or `Poetry/` — case insensitive)
      - poem1.html
      - poem2.html
    - articles/
      - article1.html
    - ukhane/
      - ukhane1.html
  - tools/
    - build_content_index.js  <-- Node script to scan src/ and create content.json
  - content.json  <-- generated by tools script, but include sample
  - .github/
    - workflows/
      - deploy.yml  <-- GitHub Actions workflow to build content.json and publish to gh-pages

Output expectations for generator (what I want you to create now):
1. A fully detailed repo skeleton and the full content of the key files: `index.html`, `category.html` (or templated behavior in `main.js`), `post.html`, `assets/css/styles.css`, `assets/js/main.js` (with code: pagination, dynamic header creation from content.json, tag filtering, search, slideshow integration), `tools/build_content_index.js`, and `.github/workflows/deploy.yml`.
2. The generator should produce working client-side code that will:
   - fetch `content.json`,
   - create nav items from distinct categories found in the manifest,
   - render the home grid with pagination (10 per page, 2 per row),
   - support "View more..." incremental loading,
   - render category pages and tag-filter pages,
   - show single-post pages (post.html reads query param e.g. `?id=...`),
   - implement slideshow using images referenced in content.json (each slide linkable).
3. Provide a README.md with:
   - instructions to add content files (how to add metadata),
   - how to run the Node helper locally (`node tools/build_content_index.js`),
   - how GH Actions will run the helper and publish,
   - how to add new categories simply by adding folders under `/src`.
4. Provide a small sample of three content files (one poem, one article, one ukhane) with proper metadata and placeholder content.
5. GitHub Actions deploy workflow: must run Node (install), run `node tools/build_content_index.js`, commit generated `content.json` (if needed) and deploy `./` to GitHub Pages using a widely used publish action. Use `GITHUB_TOKEN` for publishing.

Non-functional requirements:
- No build tools required for client side (no webpack). The Node helper is permitted for generating `content.json`.
- No back-end hosting; everything must be static and usable on GitHub Pages.
- Code must be clean, commented, and easy to adapt for future categories.
- Make sure the template supports Marathi text rendering (set UTF-8, right fonts).
- Include example meta tags for SEO and Open Graph (title, description, og:image) on `index.html` and `post.html`.

Please generate the full repository files (index.html, category/post templates, CSS, JS, the Node build script, sample content files, README, and the workflow YAML). Keep the code plain HTML/CSS/JS — no frameworks — and ensure the `tools/build_content_index.js` script is robust and well commented. Thank you.
name = "marathibytes-decap-oauth"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# GITHUB_OAUTH_CLIENT_ID, GITHUB_OAUTH_CLIENT_SECRET, and OAUTH_STATE_SECRET
# are set as secrets (`wrangler secret put <NAME>`), not here — see README.md.
